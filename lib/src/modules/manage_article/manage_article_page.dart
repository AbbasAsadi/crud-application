import 'package:flutter/material.dart';

class ManageArticlePage extends StatelessWidget {
  const ManageArticlePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ManageArticlePage'),
      ),
    );
  }
}
