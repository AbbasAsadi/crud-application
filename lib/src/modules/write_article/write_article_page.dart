import 'package:flutter/material.dart';

class WriteArticlePage extends StatelessWidget {
  const WriteArticlePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('WriteArticlePage'),
      ),
    );
  }
}
